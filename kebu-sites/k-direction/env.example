# Copy to .env (or set in Vercel). Do NOT paste JOKO payments app DATABASE_URL or Postgres here.
DATABASE_URL="file:./prisma/dev.db"
PORTAL_PASSWORD="kdirection-portal"
PORTAL_SECRET="change-me-in-production-kdirection"

# If db:setup says P1013 or shows "template1" / localhost:51214, your .env is wrong.
# Run: cp env.example .env   then   npm run db:setup

# Later: Kebu Supabase Postgres URL + supabase/migrations/ (JOKO is for mobile-money checkout only).
