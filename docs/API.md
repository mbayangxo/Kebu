# API

See [api/README.md](./api/README.md) for route index.

New in registration slice:

- `GET/POST /api/businesses/[id]/documents` — list + upload (multipart)
- `DELETE /api/businesses/[id]/documents/[docId]` — remove file + sync progress step
