# Security

See [security/README.md](./security/README.md).

Business documents: private bucket, RLS on `business_documents`, signed download URLs (1h), founder/admin upload only, 10 MB + MIME allowlist server-side.
