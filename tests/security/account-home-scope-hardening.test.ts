import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("account mutation and home scope hardening",()=>{it("requires same-origin for /api/me browser mutations",()=>{const s=readFileSync(join(process.cwd(),"middleware.ts"),"utf8");expect(s).toContain('pathname.startsWith("/api/me/")');expect(s).toContain("csrfMutationCheck")});it("does not expose personal builder projects in business context",()=>{const s=readFileSync(join(process.cwd(),"app/api/me/home/route.ts"),"utf8");expect(s).toContain("activeBusinessId");expect(s).toContain("Legacy Builder projects are personal-only");expect(s).toContain('designQuery.eq("business_id", activeBusinessId)');expect(s).toContain('designQuery.is("business_id", null)')})});