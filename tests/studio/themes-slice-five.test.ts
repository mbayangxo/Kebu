import{describe,expect,it}from"vitest";import{applyStudioTheme,KEBU_THEMES}from"@/lib/studio/themes";import{emptyCanvasDocument}from"@/lib/studio/canvas-document";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio themes slice",()=>{it("restyles without deleting pages or layers",()=>{const d=emptyCanvasDocument({width:1080,height:1080});const n=applyStudioTheme(d,KEBU_THEMES[0]!);expect(n.pages.length).toBe(d.pages.length);expect(n.pages[0]!.layers.length).toBe(d.pages[0]!.layers.length);expect(n.pages[0]!.backgroundColor).toBe(KEBU_THEMES[0]!.background)});it("ships distinct Kebu theme systems",()=>expect(KEBU_THEMES.map(x=>x.id)).toEqual(["kebu-editorial","sahel-night","gallery-cream"]));it("theme storage is workspace RLS scoped",()=>{const s=readFileSync(join(process.cwd(),"supabase/migrations/20260920113000_studio_themes.sql"),"utf8");expect(s).toContain("business_members");expect(s).toContain("owner_id=auth.uid()")});it("editor exposes themes panel",()=>expect(readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8")).toContain("StudioThemesPanel"));it("theme API derives workspace server side",()=>{const s=readFileSync(join(process.cwd(),"app/api/studio/themes/route.ts"),"utf8");expect(s).toContain("loadActiveWorkspaceScope");expect(s).not.toContain("businessId")})});