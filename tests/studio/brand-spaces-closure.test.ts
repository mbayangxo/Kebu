import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio Brand Spaces closure",()=>{it("media supports approved-only brand assets",()=>expect(readFileSync(join(process.cwd(),"app/components/studio/studio-uploads-library.tsx"),"utf8")).toContain(">Approved</button>"));it("brand space has offline cache",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-brand-space-panel.tsx"),"utf8");expect(s).toContain("readCachedBrandSpace");expect(s).toContain("Offline · using cached Brand Space.")});it("inspector exposes semantic brand colors and typography",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain("brandSpace.colors");expect(s).toContain("brandSpace.typography")});it("management edits richer rules and default",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-brand-kit-panel.tsx"),"utf8");for(const x of["Imagery direction","Brand voice","Use as default Brand Space","approvedAssetIds"])expect(s).toContain(x)})});