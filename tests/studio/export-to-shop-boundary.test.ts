import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio to Shop boundary",()=>{it("requires mutation origin, editable design access, and matching active business context",()=>{const s=readFileSync(join(process.cwd(),"app/api/create/designs/[id]/export-to-product/route.ts"),"utf8");expect(s).toContain("assertSameOriginMutation(req)");expect(s).toContain("resolveStudioDesignAccess");expect(s).toContain("!access?.canEdit");expect(s).toContain("loadActiveWorkspaceScope");expect(s).toContain("(design.business_id ?? null) !== (project.business_id ?? null)");expect(s).toContain("(design.business_id ?? null) !== (workspace.activeBusinessId ?? null)")})});