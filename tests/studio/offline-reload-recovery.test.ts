import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio offline reload recovery",()=>{it("keeps a dirty local draft, detects divergent server state, and resumes sync when safe",()=>{const s=readFileSync(join(process.cwd(),"app/studio/[id]/page.tsx"),"utf8");expect(s).toContain("setConflictServer({ doc: serverDoc, updatedAt: serverVersion })");expect(s).toContain('setSyncState("conflict")');expect(s).toContain("persistRef.current?.(docRef.current, local.designType)");expect(s).toContain("local?.dirty && !conflicts")})});