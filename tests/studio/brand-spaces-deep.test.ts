import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio deep Brand Spaces",()=>{it("editor loads Brand Space and inserts approved logos",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-brand-space-panel.tsx"),"utf8");for(const x of["Approved logos","onInsertLogo","Apply Brand Space","Imagery direction","Voice"])expect(s).toContain(x)});it("canvas connects brand space to real document mutations",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain("StudioBrandSpacePanel");expect(s).toContain('addLayer("image"')});it("default changes clear prior default inside active scope",()=>{const s=readFileSync(join(process.cwd(),"app/api/studio/brand-kit/route.ts"),"utf8");expect(s).toContain("clearDefault");expect(s).toContain('eq("business_id",businessId)');expect(s).toContain('eq("owner_id",userId)')})});