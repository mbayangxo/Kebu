import{describe,expect,it}from"vitest";import{normalizeAssetTags,searchAssets}from"@/lib/studio/assets";import{sniffStudioMedia}from"@/lib/studio/media-signature";import{searchStudioElements}from"@/lib/studio/elements-pack";import{readFileSync}from"node:fs";import{join}from"node:path";
describe("Studio assets slice",()=>{it("searches workspace asset metadata",()=>{const rows=[{id:"1",kind:"image" as const,url:"x",file_name:"Market.jpg",tags:["dakar"],favorite:true}];expect(searchAssets(rows,"dakar","image",true)).toHaveLength(1)});it("normalizes bounded tags",()=>expect(normalizeAssetTags([" Dakar ","dakar","Fashion"])).toEqual(["dakar","fashion"]));it("sniffs real file signatures",()=>{expect(sniffStudioMedia(Uint8Array.from([0x89,0x50,0x4e,0x47]),"image/png")).toBe("image/png");expect(sniffStudioMedia(new Uint8Array([1,2,3]),"image/png")).toBeNull()});it("has Kebu cultural element discovery",()=>expect(searchStudioElements("cowrie","culture")).toHaveLength(1));it("migration supports audio and metadata",()=>{const s=readFileSync(join(process.cwd(),"supabase/migrations/20260920103000_studio_asset_library.sql"),"utf8");expect(s).toContain("'audio'");expect(s).toContain("favorite");expect(s).toContain("tags text[]")});it("asset metadata mutation is workspace scoped",()=>{const s=readFileSync(join(process.cwd(),"app/api/studio/uploads/route.ts"),"utf8");expect(s).toContain("workspace.activeBusinessId");expect(s).toContain('.eq("owner_id",user.id)')})});