import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio save ordering",()=>{it("prevents an older response from overwriting newer save state",()=>{const s=readFileSync(join(process.cwd(),"app/studio/[id]/page.tsx"),"utf8");expect(s).toContain("const saveGenerationRef = useRef(0)");expect(s).toContain("const generation = ++saveGenerationRef.current");expect(s).toContain("if (generation !== saveGenerationRef.current) return;")})});