import{describe,expect,it}from"vitest";import{fillFrameWithAsset}from"@/lib/studio/asset-canvas-operations";import{canvasLayerSchema}from"@/lib/studio/canvas-document";import{readFileSync}from"node:fs";import{join}from"node:path";
describe("Studio frame + offline assets",()=>{it("keeps frame semantics when media is filled",()=>{const f=canvasLayerSchema.parse({id:"f",type:"frame",name:"Frame",x:0,y:0,width:200,height:300,rotation:0,opacity:1,locked:false,frameStyle:"rounded"});const n=fillFrameWithAsset(f,{id:"asset1",kind:"image",url:"https://x.test/a"});expect(n.type).toBe("frame");expect(n.frameMediaKind).toBe("image");expect(n.sourceAssetId).toBe("asset1");expect(n.frameFocalX).toBe(.5)});it("persists frame media and focal fields",()=>{const n=canvasLayerSchema.parse({id:"f",type:"frame",name:"f",x:0,y:0,width:100,height:100,rotation:0,opacity:1,locked:false,frameMediaKind:"video",frameMediaUrl:"https://x.test/v",frameFocalX:.2,frameFocalY:.8});expect(n.frameMediaKind).toBe("video")});it("offline cache is bounded and reconciled",()=>{const s=readFileSync(join(process.cwd(),"lib/studio/asset-offline-cache.ts"),"utf8");expect(s).toContain("MAX=150*1024*1024");expect(s).toContain("25*1024*1024");expect(s).toContain("reconcileAssetCache")});it("library falls back to cached metadata offline",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-uploads-library.tsx"),"utf8");expect(s).toContain("readCachedAssetLibrary");expect(s).toContain("resolveCachedAssetUrl");expect(s).toContain("Offline · showing cached workspace assets.")});it("frame inspector exposes focal controls",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain("Horizontal focus");expect(s).toContain("Vertical focus");expect(s).toContain("Remove frame media")})});