import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio quiet chrome and timeline",()=>{it("timeline has semantic tracks and a strong playhead",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-timeline-panel.tsx"),"utf8");expect(s).toContain("Scenes");expect(s).toContain("Audio");expect(s).toContain('bg-[#FF6A00]');expect(s).toContain("overflow-x-auto")});it("timeline keeps audio analysis and beat snap behavior",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-timeline-panel.tsx"),"utf8");expect(s).toContain("analyzeMusicFromUrl");expect(s).toContain("snapTimeToBeat");expect(s).toContain("snapToBeats")});it("editor toolbar uses quieter hierarchy",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain("border-black/[.07]");expect(s).toContain("text-black/60");expect(s).toContain("text-red-700/70")})});