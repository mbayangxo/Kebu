import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio UX contextual refinement",()=>{it("inspector uses progressive disclosure and useful empty state",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain('GalaxyInspectorSection title="Typography"');expect(s).toContain('GalaxyInspectorSection title="Brand Space"');expect(s).toContain('title="Nothing selected"')});it("Themes uses Galaxy hierarchy and state language",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-themes-panel.tsx"),"utf8");expect(s).toContain("GalaxyPanelHeader");expect(s).toContain("GalaxyStatus")});it("Brand uses the same panel and state grammar",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-brand-space-panel.tsx"),"utf8");expect(s).toContain("GalaxyPanelHeader");expect(s).toContain("GalaxyEmptyState")})});