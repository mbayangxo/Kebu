import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio media and chrome refinement",()=>{it("Media uses Galaxy hierarchy and no nested favorite button",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-uploads-library.tsx"),"utf8");expect(s).toContain('title="Media"');expect(s).toContain("GalaxyEmptyState");expect(s).not.toContain('aria-label={u.favorite?"Unfavorite":"Favorite"}')});it("toolbar uses real icons",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain('name="duplicate"');expect(s).toContain('name="trash"');expect(s).toContain("align-${t.label}")});it("mobile chrome respects safe area",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain("env(safe-area-inset-bottom)");expect(s).toContain('aria-pressed={mobilePanel==="library"}');expect(s).toContain('aria-pressed={mobilePanel==="inspector"}')})});