import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio UX refinement",()=>{it("uses Kebu Galaxy creator rail",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain("GalaxyToolRail");expect(s).toContain("STUDIO_RAIL")});it("uses code-owned accessible SVG icons",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-icons.tsx"),"utf8");expect(s).toContain('viewBox:"0 0 24 24"');expect(s).toContain('"aria-hidden":true')});it("Galaxy exposes contextual inspector and status primitives",()=>{const s=readFileSync(join(process.cwd(),"app/components/galaxy/editor-primitives.tsx"),"utf8");expect(s).toContain("GalaxyInspectorSection");expect(s).toContain("GalaxyStatus")});it("workspace changes layout deliberately at desktop breakpoint",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain("lg:grid-cols-[minmax(260px,300px)_minmax(0,1fr)_minmax(280px,320px)]")})});