import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio final UX sweep",()=>{it("video workspace uses quiet Kebu neutral hierarchy and responsive side panels",()=>{const s=readFileSync(join(process.cwd(),"app/studio/video/[id]/page.tsx"),"utf8");expect(s).toContain('bg-[#101010]');expect(s).toContain("hidden w-[220px]");expect(s).toContain("xl:block")});it("timeline clips reveal trim affordances contextually",()=>{const s=readFileSync(join(process.cwd(),"app/studio/video/[id]/page.tsx"),"utf8");expect(s).toContain('aria-label="Trim clip start"');expect(s).toContain("group-hover:opacity-100");expect(s).toContain("ring-[#FF6A00]")});it("video playhead uses Kebu accent",()=>expect(readFileSync(join(process.cwd(),"app/studio/video/[id]/page.tsx"),"utf8")).toContain('bg-[#FF6A00]'));it("canvas remains visually dominant and selection is restrained",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-canvas-editor.tsx"),"utf8");expect(s).toContain('bg-[#111214]');expect(s).toContain("outline-orange-500");expect(s).toContain('bg-[#101113]')})});