import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Studio consistency closure",()=>{it("Tools uses Galaxy and real icons",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-tools-panel.tsx"),"utf8");expect(s).toContain("GalaxyPanelHeader");expect(s).toContain("StudioIcon");expect(s).not.toContain('glyph:"Θ"')});it("media adjustments makes reset secondary and compact",()=>{const s=readFileSync(join(process.cwd(),"app/components/studio/studio-media-adjustments-panel.tsx"),"utf8");expect(s).toContain(">Reset</button>");expect(s).toContain("hover:bg-black/[.04]")});it("video timeline exposes selected and trim states accessibly",()=>{const s=readFileSync(join(process.cwd(),"app/studio/video/[id]/page.tsx"),"utf8");expect(s).toContain("aria-pressed={selected}");expect(s).toContain('aria-label="Trim clip end"')})});