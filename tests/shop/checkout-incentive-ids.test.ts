import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("checkout incentive reservation ids",()=>{it("carries resolved incentive ids from order pricing into reservation",()=>{const order=readFileSync(join(process.cwd(),"lib/shop/cart-order.ts"),"utf8");expect(order).toContain("discountId: discount?.id ?? null");expect(order).toContain("giftCardId: giftCard && giftCardAmount > 0 ? giftCard.id : null");const route=readFileSync(join(process.cwd(),"app/api/public/sites/[subdomain]/cart/checkout/route.ts"),"utf8");expect(route).toContain("discountId: lineIndex === 0 ? created.discountId : null");expect(route).toContain("giftCardId: lineIndex === 0 ? created.giftCardId : null");expect(route).not.toContain("discountResult.discount");expect(route).not.toContain("giftCardResult.card")})});