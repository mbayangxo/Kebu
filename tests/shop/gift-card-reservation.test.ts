import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("gift card checkout reservation",()=>{it("reserves gift value and consumes it only during payment commit",()=>{const s=readFileSync(join(process.cwd(),"supabase/migrations/20260923023000_checkout_reservations.sql"),"utf8");expect(s).toContain("gift_card_amount_xof");expect(s).toContain("v_gift_reserved");expect(s).toContain("balance_xof=balance_xof-r.gift_card_amount_xof");expect(s).toContain("reserved gift card unavailable at commit")})});