import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("checkout deferred incentives",()=>{it("does not spend gift cards or discount uses while merely creating an unpaid order",()=>{const order=readFileSync(join(process.cwd(),"lib/shop/cart-order.ts"),"utf8");expect(order).not.toContain("redeemGiftCardBalance(");expect(order).not.toContain("incrementDiscountUse(");const route=readFileSync(join(process.cwd(),"app/api/public/sites/[subdomain]/cart/checkout/route.ts"),"utf8");expect(route).toContain("discountId: lineIndex === 0");expect(route).toContain("giftCardId: lineIndex === 0");expect(route).toContain("giftCardAmountXof: lineIndex === 0")})});