import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("payment ledger terminal idempotency",()=>{it("deduplicates paid/refunded/cancelled events by order and event type",()=>{const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260923030000_payment_ledger_idempotency.sql"),"utf8");expect(sql).toContain("unique index");expect(sql).toContain("order_id,event_type");const lib=readFileSync(join(process.cwd(),"lib/shop/payment-ledger.ts"),"utf8");expect(lib).toContain('["paid", "refunded", "cancelled"]');expect(lib).toContain('onConflict: "order_id,event_type"');expect(lib).toContain("ignoreDuplicates: true")})});