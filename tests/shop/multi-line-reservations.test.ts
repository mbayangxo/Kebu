import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("multi-line checkout reservations",()=>{it("allows one reservation per product and commits all order lines atomically",()=>{const s=readFileSync(join(process.cwd(),"supabase/migrations/20260923023000_checkout_reservations.sql"),"utf8");expect(s).toContain("unique(order_id, product_id)");expect(s).toContain("on conflict(order_id,product_id) do update");expect(s).toContain("for r in select * from public.shop_checkout_reservations where order_id=p_order_id and status='active'");expect(s).toContain("where order_id=p_order_id and status='active'");expect(s).toContain("raise exception 'reserved stock unavailable at commit'")})});