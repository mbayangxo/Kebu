import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("provider-start reservation cleanup",()=>{it("releases inventory when no live payment session starts",()=>{const s=readFileSync(join(process.cwd(),"app/api/public/sites/[subdomain]/cart/checkout/route.ts"),"utf8");const i=s.indexOf("const started = await startShopOrderProviderCheckout");const block=s.slice(i,i+2600);expect(block).toContain("await releaseShopCheckout(admin, created.orderId)");expect(block).toContain("Inventory is not being held")})});