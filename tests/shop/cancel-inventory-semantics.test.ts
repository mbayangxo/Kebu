import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("order cancellation inventory semantics",()=>{it("releases unpaid reservations and restores stock only after a paid commit",()=>{const s=readFileSync(join(process.cwd(),"app/api/projects/[id]/orders/route.ts"),"utf8");expect(s).toContain('paymentRow?.payment_status === "paid"');expect(s).toContain("await releaseShopCheckout(admin, orderId)");expect(s).toContain("await restoreProductStock(admin")})});