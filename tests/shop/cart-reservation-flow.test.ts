import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("cart checkout inventory reservation",()=>{it("does not permanently decrement inventory before payment",()=>{const s=readFileSync(join(process.cwd(),"app/api/public/sites/[subdomain]/cart/checkout/route.ts"),"utf8");expect(s).not.toContain("decrementCartStock(");expect(s).toContain("reserveShopCheckout(admin");expect(s).toContain("releaseShopCheckout(admin, created.orderId)");expect(s).toContain('.from("shop_orders").delete()')})});