import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("refund state transition",()=>{it("only refunds paid orders, is retry-safe, and records the ledger transition",()=>{const s=readFileSync(join(process.cwd(),"app/api/projects/[id]/orders/route.ts"),"utf8");expect(s).toContain('existingRefund.payment_status !== "paid"');expect(s).toContain('.eq("payment_status", "paid")');expect(s).toContain("alreadyRefunded: true");expect(s).toContain('eventType: "refunded"')})});