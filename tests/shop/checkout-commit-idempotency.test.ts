import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("checkout commit idempotency lock",()=>{it("serializes duplicate commit and release operations for one order",()=>{const s=readFileSync(join(process.cwd(),"supabase/migrations/20260923023000_checkout_reservations.sql"),"utf8");const locks=s.match(/pg_advisory_xact_lock\(hashtextextended\(p_order_id::text, 0\)\)/g)??[];expect(locks.length).toBeGreaterThanOrEqual(2);expect(s).toContain("status='committed'");expect(s).toContain("status='active'")})});