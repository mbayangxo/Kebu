import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("active checkout reservation migration",()=>{it("ships reservation, commit and release RPCs in the active migration chain",()=>{const s=readFileSync(join(process.cwd(),"supabase/migrations/20260923023000_checkout_reservations.sql"),"utf8");expect(s).toContain("create table if not exists public.shop_checkout_reservations");expect(s).toContain("function public.reserve_shop_checkout");expect(s).toContain("function public.commit_shop_checkout");expect(s).toContain("function public.release_shop_checkout");expect(s).toContain("grant execute on function public.commit_shop_checkout")})});