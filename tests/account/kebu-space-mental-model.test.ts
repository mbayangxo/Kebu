import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Kebu space mental model",()=>{it("onboarding explains isolation before workspace naming",()=>{const s=readFileSync(join(process.cwd(),"app/components/kebu/kebu-welcome-intake.tsx"),"utf8");expect(s).toContain("Personal is your private starting space");expect(s).toContain("without mixing the work")});it("account switcher presents current space and isolation",()=>{const s=readFileSync(join(process.cwd(),"app/components/kebu-account-context-switcher.tsx"),"utf8");expect(s).toContain("Current space");expect(s).toContain("Personal and business work stay separate")});it("home exposes real business/site spaces from the account summary",()=>{const s=readFileSync(join(process.cwd(),"app/dashboard/page.tsx"),"utf8");expect(s).toContain("Your spaces");expect(s).toContain("summary.businesses.map");expect(s).toContain("summary.sites.slice")})});