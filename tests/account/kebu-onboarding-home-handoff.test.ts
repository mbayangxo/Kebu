import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Kebu onboarding handoff and honest home",()=>{it("new onboarding opens the selected first tool unless an explicit next path exists",()=>{const s=readFileSync(join(process.cwd(),"app/components/kebu/kebu-welcome-intake.tsx"),"utf8");expect(s).toContain("firstTool");expect(s).toContain("editMode || rawNext");expect(s).toContain("Start with ${firstTool.label}")});it("empty home does not fabricate activity",()=>{const s=readFileSync(join(process.cwd(),"app/dashboard/page.tsx"),"utf8");expect(s).toContain("hasWork");expect(s).toContain("Nothing is filled with demo activity");expect(s).toContain("real work will appear here automatically")})});