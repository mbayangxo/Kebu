import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("entry path performance guardrails",()=>{it("caches the global country count instead of service-role querying it per home request",()=>{const s=readFileSync(join(process.cwd(),"app/api/me/home/route.ts"),"utf8");expect(s).toContain("unstable_cache");expect(s).toContain('revalidate: 300')});it("keeps marketing template imagery on Next image optimization",()=>{const s=readFileSync(join(process.cwd(),"app/components/landing/kebu-opportunity-home.tsx"),"utf8");expect(s).not.toContain("\n                  unoptimized\n")})});