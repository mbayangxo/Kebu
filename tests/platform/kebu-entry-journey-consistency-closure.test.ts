import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Kebu entry journey consistency closure",()=>{it("dashboard actions use compact geometry and accessible attention marker",()=>{const s=readFileSync(join(process.cwd(),"app/dashboard/page.tsx"),"utf8");expect(s).toContain("aria-hidden />Needs your attention");expect(s).toContain("rounded-lg px-5 py-2.5")});it("marketing mobile chrome avoids text glyph close and pill CTAs",()=>{const s=readFileSync(join(process.cwd(),"app/components/landing/kebu-marketing-chrome.tsx"),"utf8");expect(s).toContain('aria-label="Close menu"');expect(s).toContain("<svg");expect(s).not.toContain("              ✕");expect(s).toContain("One Kebu for creating, working, selling, communicating and finding opportunities.")})});