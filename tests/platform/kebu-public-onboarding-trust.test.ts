import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Kebu public and onboarding trust pass",()=>{it("public home does not present invented builder testimonials",()=>{const s=readFileSync(join(process.cwd(),"app/components/landing/kebu-opportunity-home.tsx"),"utf8");expect(s).not.toContain("BUILDER_STORIES");expect(s).not.toContain("What builders do with it");expect(s).toContain("54");expect(s).toContain("1\", label: \"account across Kebu")});it("onboarding uses compact action geometry",()=>{const s=readFileSync(join(process.cwd(),"app/components/kebu/kebu-welcome-intake.tsx"),"utf8");expect(s).toContain('className="mt-8 rounded-lg px-6 py-3 text-sm font-semibold text-white"');expect(s).toContain("Start with ${firstTool.label}")})});