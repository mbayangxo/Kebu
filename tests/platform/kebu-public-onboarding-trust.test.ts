import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Kebu public and onboarding trust pass",()=>{it("public home avoids invented testimonials and private owner templates",()=>{const s=readFileSync(join(process.cwd(),"app/components/landing/kebu-opportunity-home.tsx"),"utf8");expect(s).not.toContain("BUILDER_STORIES");expect(s).not.toContain("What builders do with it");expect(s).not.toContain('slug: "maylecor"');expect(s).not.toContain('slug: "kdirection"');expect(s).toContain("CAPABILITIES")});it("onboarding uses compact action geometry",()=>{const s=readFileSync(join(process.cwd(),"app/components/kebu/kebu-welcome-intake.tsx"),"utf8");expect(s).toContain("Start with ${firstTool.label}");expect(s).toContain("rounded-lg")})});