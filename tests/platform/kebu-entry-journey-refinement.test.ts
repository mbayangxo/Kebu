import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Kebu entry journey refinement",()=>{it("landing presents Kebu as broader than a site builder and does not advertise private owner templates",()=>{const s=readFileSync(join(process.cwd(),"app/components/landing/kebu-opportunity-home.tsx"),"utf8");expect(s).toContain("CAPABILITIES");expect(s).toContain("Sites");expect(s).toContain("Shop");expect(s).toContain("Mail");expect(s).not.toContain('slug: "maylecor"');expect(s).not.toContain('slug: "kdirection"')});it("onboarding uses product icons instead of symbolic intent glyphs",()=>{const s=readFileSync(join(process.cwd(),"app/components/kebu/kebu-welcome-intake.tsx"),"utf8");expect(s).toContain("INTENT_ICONS");expect(s).not.toContain("INTENT_MARKS")});it("dashboard names spaces consistently",()=>{const s=readFileSync(join(process.cwd(),"app/dashboard/page.tsx"),"utf8");expect(s).toContain("Your spaces");expect(s).toContain('href="/spaces"');expect(s).not.toContain("Your worlds")})});