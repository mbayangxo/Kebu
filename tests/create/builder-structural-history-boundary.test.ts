import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder structural history boundary",()=>{it("clears props-only undo history after structural mutations",()=>{const s=readFileSync(join(process.cwd(),"app/create/[id]/page.tsx"),"utf8");for(const name of ["addSection","deleteSection","reorderSections","moveSection"]){const start=s.indexOf(`async function ${name}`);expect(start).toBeGreaterThan(-1);const next=s.indexOf("\n  async function ",start+20);const block=s.slice(start,next<0?s.length:next);expect(block).toContain("setHistory([])");expect(block).toContain("setFuture([])")}})});