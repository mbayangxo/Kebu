import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder navigation AI accessibility closure",()=>{it("navigation uses real drag affordance and compact hierarchy",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/nav-links-editor.tsx"),"utf8");expect(s).toContain("<svg");expect(s).not.toContain(">⋮⋮</span>");expect(s).toContain("focus-within:border-black/20")});it("AI panel becomes a mobile sheet and clears bottom navigation",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-site-command-bar.tsx"),"utf8");expect(s).toContain("max-h-[82dvh]");expect(s).toContain("env(safe-area-inset-bottom)");expect(s).toContain("focus-visible:ring-[#FF6A00]")});it("site motion respects reduced motion",()=>{const a=readFileSync(join(process.cwd(),"app/components/create/kebu-motion-effects.css"),"utf8");const b=readFileSync(join(process.cwd(),"app/components/create/kebu-scroll-entrance.css"),"utf8");expect(a).toContain("prefers-reduced-motion: reduce");expect(b).toContain("prefers-reduced-motion: no-preference")})});