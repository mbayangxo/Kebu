import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder quiet shell refinement",()=>{it("keeps site canvas dominant with compact chrome",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-studio-chrome.tsx"),"utf8");expect(s).toContain("h-[52px]");expect(s).toContain("sm:w-[64px]");expect(s).toContain("env(safe-area-inset-bottom)")});it("uses a restrained Kebu canvas and drop state",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-editable-preview.tsx"),"utf8");expect(s).toContain('bg-[#EEEDEA]');expect(s).toContain("Drop media here");expect(s).toContain("#FF6A00")})});