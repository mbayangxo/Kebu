import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder style and chrome refinement",()=>{it("typography and colors use compact hierarchy",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-color-panel.tsx"),"utf8");expect(s).toContain("Site colors update the working draft");expect(s).toContain("rounded-lg border p-2 text-left");expect(s).toContain("font-semibold text-black/55")});it("navigation controls are compact but retain direct canvas affordance",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/nav-size-editor.tsx"),"utf8");expect(s).toContain("space-y-3");expect(s).toContain("drag the lower edge of the navigation directly on the canvas")});it("site chrome entry cards stay quiet",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-site-chrome-panel.tsx"),"utf8");expect(s).toContain("hover:bg-black/[.02]");expect(s).toContain("text-black/55")})});