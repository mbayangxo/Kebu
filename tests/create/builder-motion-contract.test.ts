import{describe,expect,it}from"vitest";import{sectionPropsSchemas}from"@/lib/create/website-schema";describe("Builder structured motion contract",()=>{it("persists hero photo/text motion",()=>{const p=sectionPropsSchemas.hero.parse({heading:"Artist",subheading:"",buttonLabel:"",buttonHref:"#",align:"center",imageAnimation:"soft-zoom",textAnimation:"fade-up"});expect(p.imageAnimation).toBe("soft-zoom");expect(p.textAnimation).toBe("fade-up")});it("persists logo motion in navigation",()=>{const p=sectionPropsSchemas.navigation.parse({brand:"Artist",links:[],logoAnimation:"float"});expect(p.logoAnimation).toBe("float")})});