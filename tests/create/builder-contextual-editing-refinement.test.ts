import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder contextual editing refinement",()=>{it("section insertion is searchable and dismissible",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-inline-section-divider.tsx"),"utf8");expect(s).toContain("Search sections…");expect(s).toContain('e.key==="Escape"');expect(s).toContain("No sections match that search.")});it("inspector sections use quiet flat hierarchy",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-panel-section.tsx"),"utf8");expect(s).toContain("border-b border-black/[0.07]");expect(s).not.toContain(">▼</span>")});it("element inspector exposes responsive recovery",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-element-inspector.tsx"),"utf8");expect(s).toContain("Reset responsive changes");expect(s).toContain("Only this object is being edited.")})});