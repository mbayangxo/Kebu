import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder contextual Yande placement",()=>{it("opens Yande from editor chrome and never overlays a floating assistant on the site canvas",()=>{const page=readFileSync(join(process.cwd(),"app/create/[id]/page.tsx"),"utf8");const chrome=readFileSync(join(process.cwd(),"app/components/create/builder-studio-chrome.tsx"),"utf8");expect(chrome).toContain("Ask Yande");expect(chrome).toContain("onYande");expect(page).toContain("onYande={() =>");expect(page).not.toContain("Yande FAB + speed-dial");expect(page).not.toContain("yandeDialOpen")})});