import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder reorder recovery",()=>{it("does not use an uncoordinated two-request Promise.all for a swap",()=>{const s=readFileSync(join(process.cwd(),"app/create/[id]/page.tsx"),"utf8");const block=s.slice(s.indexOf("async function moveSection"),s.indexOf("async function undo"));expect(block).not.toContain("Promise.all([");expect(block).toContain("if (!second.ok)");expect(block).toContain("sortOrder: a.sort_order");expect(block).toContain("setSections(previousSections)")})});