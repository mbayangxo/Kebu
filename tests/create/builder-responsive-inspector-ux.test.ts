import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder responsive inspector UX",()=>{it("labels breakpoint-scoped editing and exposes inheritance reset",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-section-inspector.tsx"),"utf8");expect(s).toContain("Editing {responsiveDevice");expect(s).toContain("Use desktop values");expect(s).toContain("onResetResponsive")});it("wires section inspector reset to device override keys",()=>{const s=readFileSync(join(process.cwd(),"app/create/[id]/page.tsx"),"utf8");expect(s).toContain("responsiveOverrideActive={hasDeviceOverrideKeys");expect(s).toContain("DEVICE_OVERRIDE_KEYS[section.section_type");expect(s).toContain("clearDeviceOverrideKeys(")})});