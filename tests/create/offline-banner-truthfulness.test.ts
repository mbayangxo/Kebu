import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("offline banner truthfulness",()=>{it("does not announce success when queued work remains",()=>{const s=readFileSync(join(process.cwd(),"app/components/kebu-offline-banner.tsx"),"utf8");expect(s).toContain("result.failed > 0 || result.remaining > 0");expect(s).toContain("some changes still need syncing");expect(s).toContain("queued changes synced.");expect(s).not.toContain('setTimeout(() => {\n          setOffline(false)')})});