import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("navigation responsive renderer contract",()=>{it("renders breakpoint-aware navigation properties rather than desktop raw values",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/site-renderer.tsx"),"utf8");for(const key of ["navScale","navSize","navLayout","logoScale","fontFamily","fontWeight","logoAlign","navSticky","navStyle"])expect(s).toContain(`readDeviceOverride(raw, device, "${key}")`)})});