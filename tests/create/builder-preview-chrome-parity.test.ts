import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("standalone Builder preview parity",()=>{it("uses the same site-chrome composition path as publish",()=>{const s=readFileSync(join(process.cwd(),"app/create/[id]/preview/page.tsx"),"utf8");expect(s).toContain("buildEditorPreviewDefinition");expect(s).toContain("parseSiteChrome(data.siteChrome)");expect(s).not.toContain("buildDefinitionFromProjectParts(data.project, pageRows, sections)")})});