import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{join}from"node:path";describe("Builder assets apps recovery UX",()=>{it("assets use accessible status, lazy media and SVG disclosure",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/site-assets-panel.tsx"),"utf8");expect(s).toContain("GalaxyStatus");expect(s).toContain('loading="lazy"');expect(s).toContain("<svg");expect(s).not.toContain('? "▲" : "▼"')});it("apps avoid symbolic glyph tiles",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-apps-panel.tsx"),"utf8");expect(s).toContain("StudioIcon");expect(s).not.toContain("APP_MARKS")});it("version history has explicit loading and empty states",()=>{const s=readFileSync(join(process.cwd(),"app/components/create/builder-version-history-panel.tsx"),"utf8");expect(s).toContain("GalaxyStatus");expect(s).toContain("GalaxyEmptyState")})});